window:
  startup_mode: Windowed


colors:
  # Default colors
  primary:
    background: '0x000000'
    foreground: '0xffeaea'

font:
  normal:
    family: monospace
    style: Bold

  size: 16

selection:
  save_to_clipboard: true

# Default:
#   - (macOS) /bin/bash --login
#   - (Linux) user login shell
#   - (Windows) powershell
#shell:
#  program: /bin/bash
#  args:
#    - --login

mouse:
  url:
    launcher:
      program: firefox
      args: []
